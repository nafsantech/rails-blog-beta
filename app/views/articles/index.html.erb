<h2 class="text-success text-center mt-5 mb-5">List of articles</h2>
<div class="container">
<div class="shadow mb3">
	<% @article.each do |article| %>
		<h3><%= article.title %></h3>
		<p><%= article.description %></p>
		<p>
			<%= link_to 'Show', article_path(article) %>
			<%= link_to 'Edit', article_path(article) %>
			<%= link_to 'Delete', article_path(article) %>
		</p>
	<% end %>
</div>
<p><%= link_to 'Create New Article', article_path('new') %></p>
</div>
